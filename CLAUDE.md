# 심은렬 다이아만들기 (sim-diamond) — 프로젝트 컨텍스트

이 파일은 Claude Code 가 세션 시작 시 자동으로 읽는다. 이 프로젝트에서 일하기 전에
전부 읽어라.

---

## 0. 사용자와 목표

- **사용자**: 심은렬. Riot ID `F360#KR1`, KR 서버, **현재 BRONZE III**. 목표는 다이아몬드.
- **이 도구의 목적**: 본인 솔로랭크 전적을 긁어와 같은 서버 상위 티어 표본과 비교하고,
  어느 라인·습관이 발목을 잡는지 데이터로 찾아 주간 코칭 메모를 만든다.
- **개인용**. 배포하지 않고 공개 인터페이스도 없다. Riot 개인 API 키(승인됨) 사용.

### 사용자 응대 방식 (중요)

- **한국어로 답한다.**
- **추측하지 말고 실제 데이터를 보여주고 물어라.** 사용자가 명시적으로 요구한 원칙이다.
  "아마 이럴 것이다"라고 말하지 말고, 측정하거나 실행해서 결과를 근거로 말해라.
- **단계가 끝날 때마다 결과(판수·소요 시간·에러)를 보고하고 다음으로 넘어가기 전에
  확인받아라.**
- 사용자는 Windows 11 + CMD 를 쓴다. `cp`, `ls` 같은 유닉스 명령은 통하지 않는다.
  명령은 **한 줄로 붙여넣을 수 있게** 주고, 반복될 것 같으면 `scripts\*.bat` 으로 만들어라.
- 사용자가 프로그래밍 전문가는 아니다. 에러 메시지를 그대로 던지지 말고 **무엇을 하면
  되는지**를 먼저 말해라.

---

## 1. 지금 상태 (2026-09-07 기준)

### 수집 완료
- 내 매치 **368판** (2026-02-08 ~ 2026-09-05), 그중 리메이크 19판 제외 348판 집계
- 벤치마크 **45명 × 10판 = 450판** (BRONZE / SILVER / GOLD 각 15명)
- DB: `data/sim.db` — 817 매치, 8170 참가자, 238,880 프레임, 898,533 이벤트

### 진단 결과 요약 (348판)
- 승률 48.3%, KDA 3.48
- 라인 분포: 정글 259판(52.9%), 원딜 58판(41.4%), 서폿 23판(21.7%), 탑 5판⚠, 미드 3판⚠
- 정글 승률 52.9% 는 GOLD 벤치(53.3%)와 사실상 동률
- **확실히 밀리는 것**: 제어 와드를 산 판 비율(정글 38.6% vs GOLD 84.4%), 적정글 체류
  (1.49분 vs BRONZE 3.46), 라인전 15분 골드(정글 외 전 라인 마이너스)
- **문제가 아닌 것**: 판당 데스 6.49 = BRONZE 벤치와 동일. 시야 없이 죽은 비율 67.0%는
  GOLD(69.0%)보다 오히려 낮다. (초기에 이걸 문제로 오판했다가 벤치 비교를 붙이고 뒤집혔다)

### 방향 전환 — 탐색 단계
주 라인을 **확정하지 않았다.** 5개 라인을 정해진 순서로 각 15판씩, 총 75판 돌려보며
데이터로 고른다. 확정된 설정:

| 순서 | 라인 | 기준 챔프 |
|---|---|---|
| 1 | TOP | Garen, Malphite |
| 2 | MIDDLE | Annie, Malzahar |
| 3 | BOTTOM | MissFortune, Ashe |
| 4 | UTILITY | Leona, Sona |
| 5 | JUNGLE | Rammus, Amumu |

**탐색 75판이 끝나기 전에는 어떤 라인도 추천하지 않는다.** 코치 메모 시스템
프롬프트에도 박혀 있고, 사람이 답할 때도 같은 규칙을 지켜라.

### 아직 안 한 것
1. **코치 메모 실제 생성** — `ANTHROPIC_API_KEY` 가 없어 한 번도 실행 못 했다.
2. 탐색 경기 0판 (단계만 등록됨)
3. 리플레이(.replay) 파싱 — 와드 좌표 실측. 3단계 후보.

---

## 2. 지금 막혀 있는 것 (최우선)

사용자 PC 에서 `git pull` 이 실패한다:

```
error: Your local changes to the following files would be overwritten by merge:
        .env.example
```

`.env.example`(git 이 관리하는 빈 템플릿)에 실제 키를 넣어서 생긴 충돌이다. 해결:

```bat
copy /Y .env.example .env.example.bak
git checkout -- .env.example
git pull
```

`.env.example.bak` 에 원본이 남으니 `.env` 에 키가 없으면 거기서 복사한다.
`.env` 와 `data/sim.db` 는 `.gitignore` 대상이라 안전하다.

**그리고 라이엇 API 키가 채팅 스크린샷으로 노출됐다.** developer.riotgames.com 에서
`새로운 API 키`로 재발급하고 `.env` 를 갱신하라고 사용자에게 알려라.

---

## 3. 환경

### 두 개의 API 키 (사용자가 한 번 헷갈렸다)

| 변수 | 발급처 | 형태 | 용도 |
|---|---|---|---|
| `RIOT_API_KEY` | developer.riotgames.com | `RGAPI-...` | 전적 수집 |
| `ANTHROPIC_API_KEY` | console.anthropic.com | `sk-ant-...` | 코치 메모 (유료) |

개인 키가 승인돼 **24시간 만료가 없다.** 레이트리밋 20/1s, 100/2min (코드 설정과 일치).

### `.env` vs `.env.example`
- `.env` = 진짜 설정. `.gitignore` 대상. **키는 여기에만.**
- `.env.example` = git 이 관리하는 빈 템플릿. **여기에 키를 넣으면 pull 이 막히고
  커밋하면 공개된다.** `doctor` 가 이 실수를 잡아준다.

### Windows 함정
- 메모장이 `.env` 를 `.env.txt` 로 저장한다 → `doctor` 가 감지해서 `ren` 명령을 알려준다
- 콘솔 인코딩(cp949)으로 죽지 않게 `config.py` 가 stdout 을 `errors="replace"` 로 설정
- `cp`/`ls`/`cat` 안 됨. `copy`/`dir`/`type`

---

## 4. 구조

```
sim_diamond/
  config.py         .env 로딩, 라우팅/레이트리밋 상수, 리메이크 기준
  riot_client.py    레이트리밋 + 429/5xx 재시도 + SQLite 캐시
  db.py             스키마, upsert, 마이그레이션(컬럼 자동 추가)
  parse.py          raw JSON → matches/participants/frames/events
  geo.py            좌표 → 구역 분류
  metrics.py        참가자 1명·1경기 지표 → participant_metrics, deaths_detail
  ddragon.py        챔피언/아이템 정적 데이터(ko_KR)
  collect_me.py     내 계정 수집
  collect_bench.py  벤치마크 표본 수집
  phase.py          탐색/집중 단계 관리
  rate.py           경기별 재미 점수 기록
  explore.py        탐색 진행 현황 · 적합도 계산
  report.py         마크다운 리포트
  coach.py          코치 메모 (Claude API)
  check.py          수집 데이터 점검
  doctor.py         .env / API 키 진단
  status.py         진단 결과를 logs/status.txt 로 모음
scripts/            .bat(Windows) / .sh 래퍼
tests/              합성 픽스처 + 전 구간 검증 (네트워크 불필요)
```

### 명령

```bat
scripts\update.bat            REM 최신 코드 받기
scripts\send.bat              REM 진단 결과를 logs/status.txt 로 모아 push
python -m sim_diamond.doctor  REM .env / 키 진단
python -m sim_diamond.check   REM 수집 데이터 점검 (이상 있으면 종료코드 1)
python tests/test_pipeline.py REM 네트워크·키 없이 전 구간 검증 (1초)

python -m sim_diamond.collect_me [--limit N] [--recompute] [--include-normals]
python -m sim_diamond.collect_bench --tiers BRONZE SILVER GOLD
python -m sim_diamond.phase init-explore --top ... --jungle ... (기준 챔프)
python -m sim_diamond.rate 4 "메모"
python -m sim_diamond.explore
python -m sim_diamond.report
python -m sim_diamond.coach [--dry-run] [--show]
```

---

## 5. 설계 결정과 근거 (되돌리기 전에 읽어라)

전부 **실측 데이터를 보고** 정한 것이다. 사양서와 다른 부분이 있고, 이유가 있다.

| 결정 | 값 | 왜 |
|---|---|---|
| 첫 풀캠프 임계값 | 정글 CS **20** | 사양은 12였다. 실측 곡선상 12는 2분에 걸리는데 캠프는 1:30에 생긴다. 6캠프 총 몬스터가 ≈20마리라 20이 맞다 (`FULL_CLEAR_JUNGLE_CS`) |
| 첫 갱 정의 | 3분 이후 **라인(탑/미드/봇)에서 딴 킬/어시** | 원래 "킬/어시 또는 데스"였는데 내 정글에서 털려 죽은 게 갱으로 잡혔다. 사용자 지시로 분리 |
| 첫 카정 피해 | 내 정글에서 내가 죽은 첫 시각, **시간 하한 없음** | 1:30 인베이드가 이 지표의 핵심이라 3분 하한을 뺐다 |
| 오브젝트 참여 반경 | **1200** (r800/r2500 병기) | 2500은 "근처에 있었다"가 "참여했다"로 뭉개진다. 처치 ±30초 창의 최단거리로 판정 |
| 리메이크 | 5분 미만 **집계 제외** | 74초짜리 패배 한 판이 20판 표본 승률을 5%p 흔들었다 |
| 귀환 분류 | "직전 상점 방문 이후 죽었는가" | 고정 75초 창은 부활 시간이 10~50초로 변해 오분류가 많았다 |
| 적합도 정규화 축 | BRONZE=0, GOLD=1 | 분당 CS(6.4)와 골드 차이(−390)는 단위가 달라 못 더한다. "브론즈→골드 얼마나 왔나"가 이 프로젝트의 질문 자체다. 두 벤치가 사실상 같은 지표는 변별력이 없어 제외 |
| 적합도 가중 | 지표 70% + 재미 30% | 사용자 지정 |
| 탐색 순서 | TOP→MIDDLE→BOTTOM→UTILITY→JUNGLE | 사용자 지정. 순서 위반은 **그 경기를 한 시점의 현재 라인**과 비교해 판정 |
| 챔프 위반 vs 순서 위반 | 챔프 위반은 진행에서 **제외**, 순서 위반은 **포함하되 표시** | 순서가 틀려도 그 라인 기준 챔프 연습은 실제로 쌓인다 |
| 표본 기준 | 15판 미만 = "표본 부족" ⚠ | 탐색 목표와 같은 값 |

### 근사가 들어간 지표 (리포트에 `_(추정)_` 표기)

Riot 이 주지 않는 정보라 추정한다. **절대 수치보다 벤치와의 차이를 봐야 한다.**

| 항목 | 왜 근사인가 | 방법 |
|---|---|---|
| 데스 시 아군 와드 유무 | `WARD_PLACED` 에 **좌표가 없다** | 설치자의 그 시각 위치로 대체(프레임+킬 이벤트 좌표 선형보간) |
| 오브젝트 참여 | 처치 시점 내 좌표가 없다 | 처치 ±30초 창의 최단거리. 처치자·어시스트는 무조건 참여 |
| 첫 풀캠프 | 캠프별 이벤트가 없다 | 정글 CS 20 도달 시각을 프레임 사이 보간 |
| 귀환 | 귀환 이벤트가 없다 | 상점 구매를 20초 간격으로 묶어 기지 방문으로 |

**리플레이(.replay)에는 와드 정확 좌표가 있다.** `/lol/match/v5/matches/by-puuid/{puuid}/replays`
가 200을 준다(확인함). 단 **최근 5판만**, S3 서명 링크는 **1시간 만료**. ROFL 파서가 따로
필요해 3단계 후보로 미뤘다.

---

## 6. 실전에서 나왔던 버그 (재발 방지)

전부 실제 데이터를 돌려서 발견했다. 비슷한 걸 또 만들지 마라.

1. **껍데기 매치** — 라이엇이 시작되지 않은 게임을 `queueId 0, duration 0, 참가자 0명`
   으로 준다. `matches` 에 저장하지 않는다(원본은 `matches_raw` 에 보존).
2. **증분 계산이 매치 단위였다** — 한 매치에 추적 대상이 2명 이상이면 두 번째가 영영
   계산 안 됐다. 인원수를 비교하도록 고쳤다.
3. **리메이크가 데스 집계에서 안 빠졌다** — 분모만 빠져 판당 데스가 부풀려졌다.
4. **벤치 열이 SILVER/GOLD 하드코딩** — BRONZE 를 모아도 리포트에서 조용히 사라졌다.
   지금은 수집한 티어만큼 열이 생기고 실력 순으로 정렬된다.
5. **각주가 상수를 안 따라갔다** — 임계값을 20으로 바꿨는데 문구는 12였다. 문자열에
   숫자를 하드코딩하지 말고 상수를 참조해라.
6. **`--limit 20` 인데 100개씩 요청** — 남은 개수만큼만 요청한다.
7. **의도한 4xx 를 실패로 카운트** — `/replays` 탐색의 403 때문에 정상 실행이 "실패
   1회"로 보였다. `handled` 카운터로 분리.

---

## 7. 검증

```bat
python tests\test_pipeline.py
```

**네트워크도 API 키도 필요 없다.** `tests/fixtures.py` 가 Riot 응답 구조를 그대로 흉내낸
합성 match/timeline JSON 을 만들고, 파싱 → 지표 → 리포트 → 탐색 집계 전 구간을 1초 안에
돌려 60여 개 항목을 검증한다.

**코드를 고쳤으면 반드시 이걸 돌려라.** 실패하면 픽스처가 실제 구조를 반영하는지부터
확인해라 — 지금까지 테스트 실패의 절반은 코드가 아니라 테스트 기대값이 틀린 경우였다.

새 지표를 추가하면 픽스처에 그 지표가 실제로 갈리는 케이스를 넣어라. 예: 오브젝트
반경을 나눌 때 "중간 거리(약 2000)" 오브젝트를 픽스처에 넣어 r800/r1200 에서는 빠지고
r2500 에서만 잡히는 걸 검증한다.

---

## 8. 다음에 할 일 (순서대로)

1. **git pull 막힘 해결** (2장) + 라이엇 키 재발급
2. `python -m sim_diamond.phase init-explore ...` 재실행 — 지금 DB 의 단계는 구버전이라
   `role_order` 가 없다. 탐색 0판이라 잃을 게 없다.
3. **코치 메모 첫 실행** — `.env` 에 `ANTHROPIC_API_KEY` 추가 후
   `python -m sim_diamond.coach --dry-run` 으로 프롬프트 확인 → `coach` 실행.
   출력이 900자 이내인지, 존댓말인지, "이번 주 과제"가 정확히 1개인지, 라인 추천을
   안 하는지 확인하고 어긋나면 `coach.SYSTEM_PROMPT` 를 조여라.
4. 탐색 진행 — 경기마다 `python -m sim_diamond.rate <1-5> "메모"`,
   주기적으로 `collect_me` + `report` + `coach`
5. 75판 완료 후 라인 결정 → 집중 단계(`phase add`)
6. (선택) 리플레이 파싱으로 와드 좌표 실측

---

## 9. 코드 스타일

- 주석과 문서는 **한국어**. 커밋 메시지도 한국어.
- 주석은 **왜**를 적는다. 무엇을 하는지는 코드가 말한다.
- 상수는 모듈 상단에 모으고 근거를 주석으로 남긴다 (`metrics.py` 상단 참고).
- 새 지표 컬럼은 `db.METRIC_COLUMNS` 에 추가하면 `_migrate_metrics` 가 기존 DB 에
  자동으로 붙인다. 사용자에게 DB 를 지우게 하지 마라.
- raw JSON 은 절대 버리지 않는다. 파싱 결과는 raw 만 보고 재생성 가능해야 한다.
