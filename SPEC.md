# sim-diamond 사양서 (SPEC) — 사용자가 정한 것

> 이 문서는 **심은렬 본인이 작성한 요구사항**이다. `CLAUDE.md` 가 "어떻게 만들었나"라면
> 이 문서는 "무엇을 왜 원하는가"다. 둘이 충돌하면 **이 문서가 우선**이고,
> 여기 없는 결정은 혼자 하지 말고 사용자에게 물어라.

---

## 이 프로젝트가 뭔가

심은렬(롤 계정 F360#KR1, KR 서버, 현재 브론즈)의 개인 롤 코치 프로그램이다.
목표는 다이아. 상위 2~3%라 한 시즌 목표가 아니고, 주 20판 × 몇 시즌짜리 장기 프로젝트다.
사용자는 1명(나)뿐이고 배포 안 한다. 로컬 윈도우 PC에서만 돈다.

## 내가 원하는 것 (우선순위 순)

1. 진짜 프로 코치가 내 경기를 보고 말해주듯이, 내 데이터에 근거한 맞춤 진단과 훈련 과제.
   일반론("시야 잡으세요")은 필요 없다. "너는 367판 중 231판에서 제어 와드를 안 샀다"가 필요하다.
2. 매 판 끝나고 30초 안에 끝나는 루틴. 루틴이 귀찮으면 안 지켜지고, 안 지켜지면 이 프로그램은 죽는다.
3. 한 번에 고칠 것은 딱 하나. 브론즈에서 열 개를 고치라고 하면 아무것도 안 고쳐진다.
4. 숫자 없는 조언 금지. 벤치마크(브론즈/실버/골드 표본)와 비교해서 격차가 있는 것만 말해라.

## 지금까지 정한 것 (바꾸지 말 것, 바꾸려면 나한테 물어라)

- 라인은 아직 정해지지 않았다. 정글 259판이 있지만 그건 "익숙한 것"이지 "맞는 것"의 증거가 아니다.
  5개 라인을 전부 탐색한다: 탑 → 미드 → 원딜 → 서폿 → 정글 순서, 라인당 15판, 총 75판.
  정글을 마지막에 두는 이유: 익숙한 데로 도망가는 걸 막기 위해서.
- 탐색은 라인당 "기준 챔프" 2개로만 한다. 탑 가렌/말파이트, 정글 람머스/아무무, 미드 애니/말자하,
  원딜 미스포츈/애쉬, 서폿 레오나/소나. 이건 추천이 아니라 측정 도구다. 조작이 단순해서
  결과가 챔프 성능이 아니라 내 판단력을 반영하도록 고른 것. 녹턴(189판, 59%)은 숙련도 편향 때문에 일부러 뺐다.
- 적성 판정은 승률이 아니라 절대 지표(CS, 데스 시점, 시야, 골드차)를 골드 벤치와 비교해서 한다.
  적합도 = 지표 70% + 내가 매 판 입력하는 재미 점수(1~5) 30%. 축은 브론즈 벤치=0, 골드 벤치=1.
- 75판 끝나기 전에는 라인 추천을 하지 않는다. 15판 미만 표본으로 결론 내지 않는다.
- 챔프 폭은 라인 확정 후 그 라인 안에서 유형별로 탐색한다. 지금은 손대지 않는다.
- 벤치마크는 항상 "현재 티어 +1"과 비교한다. 다이아와 바로 비교하면 전부 빨간불이라 우선순위가 안 보인다.
  브론즈→실버→골드→플래→에메→다이아 5단계 졸업제, 단계마다 졸업 조건이 있다.
- 이번 주 과제: 제어 와드 매 귀환마다 1개, 판당 2개 이상. 다음 주 후보: 25분 이후 데스 줄이기.

## 데이터에서 이미 확인된 것 (2026년 솔랭 348판 기준)

- 정글 52.9%, 원딜 41.4%, 서폿 21.7%(23판). 오프 라인이 전체 승률을 48.3%로 끌어내림.
- 첫 풀캠프 3.58분, 첫 갱 6.81분 — 골드급. 기본 루틴은 있다.
- 제어 와드: 정글에서 한 개라도 산 판이 38.6% (골드 84.8%). 첫 구매 10.4분 (골드 7.2분).
- 카정 체류 1.49분/판 — 브론즈 벤치(3.46)의 절반 이하.
- 데스: 판당 6.49로 브론즈 평균과 같음. 0~15분은 벤치보다 적게 죽는데 25분 이후 데스가 유일하게 벤치를 넘음.
  → 라인전이 아니라 후반 판단이 문제.
- 라이너로 했을 때 15분 골드 전부 마이너스 (원딜 −390, 10분 CS차 −5.6).

## 대시보드 UI (2026-09-07 지정)

- 챔프·아이템 이미지 자리는 **정사각형 플레이스홀더**로 두고 안에 **챔프명 텍스트**를 넣는다.
  실제 이미지는 나중에 **Data Dragon CDN 주소로 교체**한다. 지금 CDN 을 붙이지 않는다.
- **챔프 아이콘은 64px 원형.**
- **「오늘」 화면 상단**의 기준 챔프 2개는 **스플래시를 배경으로 깔고 어둡게 처리**한다.

이유: 판수가 재료라 화면 구조가 확정되기 전에 이미지 에셋을 붙이면 두 번 일한다.
플레이스홀더에 챔프명을 넣어두면 CDN 없이도 레이아웃과 기준 챔프 표시를 검증할 수 있다.

## 코치 말투

한국어, 존댓말. "매 귀환마다 제어 와드를 1개 사세요." 처럼. 반말("사라") 금지.
과제는 "무엇을 / 측정 목표치 / 다음 리포트에서 확인할 숫자" 세 요소를 반드시 포함.
분량 900자 이내. 근거 수치는 빼지 말고 설명을 줄여라.

## 하지 말 것

- 내가 시키지 않은 기능 추가. 지금은 도구가 아니라 판수가 재료다. 개발이 게임 시간을 잡아먹으면 실패다.
- 정글로 확정하라는 식의 조언. 데이터가 그렇게 보여도 75판 전엔 금지.
- 리포트에 없는 숫자 만들기. 근사값(와드 좌표, 오브젝트 참여 위치, 귀환 판정)은 반드시 "(추정)" 표기.
- 사양에 없는 결정을 혼자 하기. 판단이 필요하면 근거를 보여주고 나한테 물어라.

## 현재 상태

1단계(수집·지표·벤치·리포트) 완료. 2단계(Claude API 코치 메모) 완료.
탐색 단계 시작됨(init-explore 실행). 다음 작업은 로컬 대시보드 UI: FastAPI 5개 엔드포인트
(/today /explore /report /coach /rate) + 클로드 디자인 프로토타입 연결. 화면은 4개(오늘/탐색/진단/코치)뿐.
